{
    "name": "electron",
    "version": "1.0.0",
    "main": "dist/main.js",
    "scripts": {
        "start": "npm run build && electron dist/main.js",
        "build": "tsc",
        "watch": "tsc -w"
    },
    "devDependencies": {
        "electron": "^12.0.0",
        "electron-builder": "^22.10.5"
    },
    "dependencies": {
        "axios": "^0.21.1"
    }
}